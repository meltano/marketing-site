/* eslint-disable no-inner-declarations */
/**
 * WordPress-driven preheader was wired via Gatsby useStaticQuery.
 * Re-enable by passing `preHeader` from a server component or restoring WP fetch here.
 */
export default function Preheader() {
  return null
}
